import TerminalDockerRunVolume from '/snippets/install/terminal-docker-run-volume.mdx';

Make sure your Docker daemon is running. Copy and paste the commands to start Bytebase.

<TerminalDockerRunVolume />

Bytebase is now running via Docker, and you can access it via `localhost:8080`. Register the first admin account which will be granted [`Workspace Admin`](/concepts/roles-and-permissions).
