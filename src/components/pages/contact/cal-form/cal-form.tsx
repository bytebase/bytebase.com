'use client';

import Cal from '@calcom/embed-react';

const CalForm = () => {
  return <Cal calLink="tianzhou/bytebase-product-walkthrough" config={{ theme: 'light' }} />;
};

export default CalForm;
